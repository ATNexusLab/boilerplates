export default function Home() {
  return (
    <div className="app">
      <h1>{{PROJECT_NAME}}</h1>
      <p>Built with Vinext + Vite + TypeScript</p>
      <p>Ready to deploy on Cloudflare Workers</p>
    </div>
  );
}
