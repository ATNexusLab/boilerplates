{
  "dependencies": {
    "@prisma/client": "^6.2"
  },
  "devDependencies": {
    "prisma": "^6.2"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev"
  }
}
